/**
 * 「実装済みだが導線を出していない機能」を利用者へどう伝えるかの単一ソース。
 *
 * 🔴 なぜ 1 ファイルに集めるか
 * 「近日公開」は放っておくと【果たされない約束】として本番に residue のまま残る。散らばって
 * いると、機能を出すときにも取り下げるときにも消し漏れる。ここに集約しておけば、
 * 対応する Issue を閉じるときにこのファイルを開くだけで棚卸しできる。
 *
 * ⚠️ ここへ足すのは【神原さんが「機能は出さないが表示は出す」と決めたもの】だけにすること。
 * 未完成の機能を隠すための一般的な逃げ道にしない。
 *
 * 現在の 2 件はどちらも 2026年7月5日の決定で、API・テストまで整備済みだがフロントの導線が
 * 無い（＝利用者からは存在しないのと同じ）機能。決定は「今回は機能を完成させない。
 * フロントには近日公開的な表示のみ出す」で、その表示だけが未実施のまま残っていた。
 */

/** オンライン前払い決済（Issue #408）。決済導線そのものは出さない、という決定に従う。 */
export const ONLINE_PREPAYMENT_NOTICE =
  'お支払いは店舗にてお願いします（オンライン前払いは近日公開予定）';

/** キャンセル待ち登録（Issue #409）。登録 UI は出さず、予定だけを伝える。 */
export const WAITLIST_NOTICE = 'キャンセル待ちのご登録は近日公開予定です。';
