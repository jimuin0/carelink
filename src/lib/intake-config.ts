/**
 * 問診（intake）機能の顧客向け公開ゲート（監査M2/H1・2026年7月22日 神原さん決定）。
 *
 * 【背景】送信済み問診票（intake_form_responses・PII）を施設スタッフが閲覧する管理画面が
 * アプリ内に存在せず（PII のサイレント死蔵）、かつゲスト予約からの問診送信が booking_id 付きで
 * 必ず 401 になる（H1）状態でローンチを迎える。神原さんの判断で、閲覧UIを作るのではなく
 * 【ローンチでは問診機能を顧客に出さない（非表示）】方針とした。
 *
 * この定数を単一ソースとして顧客導線（予約完了バナー・/intake フォーム・POST /api/intake）を
 * 一括でゲートする。閲覧UI（M2）を実装して問診を再開する際は true に戻すだけでよい（可逆）。
 * 管理側のテンプレ定義データ・テーブル・RLS には手を触れないため、再開時にデータは保全される。
 */
export const INTAKE_CUSTOMER_ENABLED = false;
