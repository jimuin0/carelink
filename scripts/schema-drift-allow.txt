# スキーマドリフト突合の除外リスト（scripts/schema-diff.mjs --allow が読む）
#
# 書式: <行の前方一致パターン> ## <なぜ除外して安全か>
# 🔴 理由が空の行は【無効】（「印を付ければ通る」にしない）。
#
# 除外してよいのは「Supabase が管理していて migration に現れないもの」だけ。
# 本番にだけ在るテーブル/列/ポリシーを「面倒だから」で入れないこと。
# それは out-of-band 変更そのもので、この監視が検知したい対象。
#
# 導入時点(2026年8月2日)は【1 件も無い】。本番との初回突合で出た差分は、
# まず「本当に Supabase 管理か」を確認し、そうでなければ migration 側を直すこと。
